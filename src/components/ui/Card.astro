---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["bg-card text-card-foreground rounded-xl border border-border shadow-sm overflow-hidden", className]}>
  <slot />
</div>
